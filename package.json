{
    "scripts": {
      "clean": "rm -rf lib/common/dist backend/dist frontend/dist",
      "build:common": "cd lib/common && npm install && tsc --build",
      "build:backend": "cd backend && npm install && npm run clean && tsc --build",
      "build:frontend": "cd frontend && npm install && npm run clean && tsc --build",
      "build": "npm run clean && npm run build:common && npm run build:backend && npm run build:frontend"
    }
  }
  